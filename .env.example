# Placeholder for SvelteKit/public environment variables.
# Do not put real secrets in this file.
PUBLIC_APP_NAME="rankings.trade"

# Clerk publishable key (client-safe). Exposed to the browser via the PUBLIC_ prefix.
# Find it on the Clerk dashboard API keys page. Name only — no real value here.
PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_replace-with-your-publishable-key"
